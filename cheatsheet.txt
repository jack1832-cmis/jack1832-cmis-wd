ls = list
cd filename= moving between folders
cd = back to home folder
cd.. = back to mother folder
gedit filename opens files
git add filename 
git commit -m "comment"
python -m SimpleHTTPServer
sftp username@cmis-hsteacher = sftp
put filename = to production server
put * = everything in folder
put *.txt .jpg etc = everything in file of that type
rm = remove files
exit = exit sftp
view index from development server = localhost:8000
create new folder : mkdir (new folder name) list and links
command sftp : sftp username@cmis-hsteacher
cd repository
command : put
remove file : rm filename
 

